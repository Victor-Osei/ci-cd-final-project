Flask==2.1.2
Werkzeug==2.1.2
python-dotenv==0.20.0

gunicorn==20.1.0
honcho==1.1.0

# Linting & formatting
flake8==4.0.1
black==22.3.0
pylint==2.14.0

# Testing
pytest==8.4.2
pytest-cov==7.1.0

# Critical fix (prevents your crash)
MarkupSafe==2.0.1

# Optional (only if really needed)
requests==2.32.5